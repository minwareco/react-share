name: Release

on:
  # Disabled: upstream release workflow; minwareco does not publish this package to NPM
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled'
        required: false

jobs:
  release:
    name: Release
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
